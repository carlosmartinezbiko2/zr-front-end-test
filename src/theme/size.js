export const gridBaseSize = 4 //px

export const sizes = {
  xtiny: gridBaseSize,
  tiny: gridBaseSize * 2,
  mini: gridBaseSize * 3,
  small: gridBaseSize * 4,
  base: gridBaseSize * 6,
  medium: gridBaseSize * 8,
  large: gridBaseSize * 12,
  xlarge: gridBaseSize * 16,
  xxlarge: gridBaseSize * 24,
}
